export type {
  DeviceType,
  AspectRatio,
  GravityType,
  HeroImageConfig,
  HeroImageUrls,
  HeroImage,
  HeroImageConfigMap,
  HeroImageKey,
} from "./hero-image"

export type Image {
    url: string;
    alt: string;
}